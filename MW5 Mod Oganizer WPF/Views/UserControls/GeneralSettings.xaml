<UserControl x:Class="MW5_Mod_Organizer_WPF.Views.UserControls.GeneralSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MW5_Mod_Organizer_WPF.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:Background="Beige">
    <Grid Margin="20">
        <StackPanel>
            <Label Content="Game info" Foreground="#484D54" FontSize="20" FontWeight="Bold"/>
            <Separator Margin="0 0 0 10"/>
            <TextBox Text="{Binding GameVersion, UpdateSourceTrigger=PropertyChanged}" Height="25" VerticalContentAlignment="Center" HorizontalAlignment="Left" VerticalAlignment="Center" Style="{StaticResource TextBoxGameVersionTheme}">
                <ToolTipService.ToolTip>
                    <ToolTip Content="Sets the game version of your mods. Should be set as the same version your game is running at."/>
                </ToolTipService.ToolTip>
            </TextBox>
        </StackPanel>
    </Grid>
</UserControl>
